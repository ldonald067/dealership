Show or adjust the project's color palette and theme.

If the user asks to "show" the theme:
1. Read `tailwind.config.ts` and `src/app/globals.css` for current theme values
2. List all custom colors with their hex values and where they're used
3. Describe the current vibe/mood the palette creates

If the user asks to "change" or "try" something:
1. Read the current theme files
2. Apply the requested changes to `tailwind.config.ts` and/or `globals.css`
3. Preview a key page to show the result
4. Show before/after if possible

Guidelines:
- Warm tones are preferred (ambers, warm grays, terracotta, sage, cream)
- Avoid cold corporate blues and stark grays
- Ensure sufficient contrast for accessibility (WCAG AA minimum)
- Dark mode should feel cozy, not harsh (dark warm grays, not pure black)