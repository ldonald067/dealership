Analyze the current conversation for non-obvious learnings, gotchas, or patterns worth documenting. For each insight found:

1. Determine which area it belongs to (auth, database, UI, lease logic, uploads, etc.)
2. Check if a relevant doc already exists in `docs/`
3. Either update the existing doc or create a new one
4. Keep entries concise — focus on the "what" and "why", not prose

Format for gotcha entries:
```
## Short Title
Brief description of the gotcha or pattern.
**Fix/Workaround:** What to do about it.
```

After saving, report what was captured and where it was saved.