Run through the full customer-to-dealer workflow end-to-end to verify everything works:

1. Check if the dev server is running (start it if not)
2. Test customer registration and login via the API
3. Test vehicle browsing endpoint
4. Test document upload
5. Test credit application submission
6. Test appointment booking
7. Test dealer login and review actions
8. Test status updates

Report each step as pass/fail with details on any failures. Use the API endpoints directly (curl/fetch) rather than browser testing.